% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gating_designer.R
\name{design_gating_template}
\alias{design_gating_template}
\title{Launch a visual gating-template designer.}
\usage{
design_gating_template(
  data_path,
  output_file = file.path(data_path, "gt_samples.csv"),
  template_file = if (file.exists(output_file)) {
     output_file
 } else {
    
    system.file("tools", "gt_samples.csv", package = "expressalyzr", mustWork = TRUE)
 },
  template_files = NULL,
  output_files = NULL,
  template_labels = NULL,
  gate_alias = "nondebris",
  parent = "root",
  dims = c("FSC-A", "SSC-A"),
  sample = NULL,
  max_events = 10000L,
  launch = interactive()
)
}
\arguments{
\item{data_path}{Experiment directory containing a \code{data/} folder of
FCS files.}

\item{output_file}{Gating template path to write. Defaults to
\code{gt_samples.csv} in \code{data_path}.}

\item{template_file}{Existing template path to seed the editor. Defaults to
\code{output_file} when it exists, otherwise the package template.}

\item{template_files}{Optional character vector of template files to edit in
one combined app. When provided, users can switch between templates in the
sidebar and each template is saved to its matching \code{output_files}
path.}

\item{output_files}{Optional output paths matching \code{template_files}.
Defaults to \code{template_files}.}

\item{template_labels}{Optional user-facing labels for
\code{template_files}, such as population names.}

\item{gate_alias}{Initial gate row alias to select.}

\item{parent}{Parent population used when creating a new boundary gate.}

\item{dims}{Initial two channels to plot and gate.}

\item{sample}{Optional sample name to show initially.}

\item{max_events}{Maximum events to plot per sample.}

\item{launch}{Whether to run the app. Set \code{FALSE} to return the Shiny
app object for testing or embedding.}
}
\value{
Runs the app when \code{launch = TRUE}; otherwise returns a
  \code{shiny.appobj}.
}
\description{
Opens a Shiny/plotly app for editing \code{gt_samples.csv}. Boundary gates
can be adjusted visually on any selected template row; automated methods such
as \code{mixture_gate} and \code{singletGate} can be edited as template rows.
The plot can switch between two-channel scatter and one-channel histogram
counts views, and saved gates remain overlaid while switching samples so
users can check whether the gate generalizes across wells.
}
